# Plant Archive V2 发布到 GitHub Pages

## 需要上传的文件

- `index.html`
- `plant-archive-v2.html`
- `plant-hero-morning.png`
- `apple-touch-icon.png`
- `site.webmanifest`

## 最短步骤

1. 在 GitHub 新建一个仓库，比如 `plant-archive-v2`
2. 把上面的文件上传到仓库根目录
3. 进入 GitHub 仓库的 `Settings`
4. 打开 `Pages`
5. `Build and deployment` 选择:
   - `Source`: `Deploy from a branch`
   - `Branch`: `main`
   - `Folder`: `/ (root)`
6. 保存后，等待 GitHub Pages 生成地址

## 手机添加到主屏幕

1. 用 iPhone Safari 打开 GitHub Pages 地址
2. 点击分享按钮
3. 选择 `添加到主屏幕`
4. 名称可以改成 `植物档案 V2`

## 说明

- `index.html` 是 GitHub Pages 的默认入口
- 页面已经补了 `manifest`、`theme-color` 和 `apple-touch-icon`
- iPhone 添加到主屏幕后，会比普通网页更像一个独立小应用
