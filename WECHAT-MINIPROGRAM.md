# 植物档案 V2 微信小程序上线说明

这个目录已经补齐原生微信小程序工程文件，可以直接用微信开发者工具导入。

`project.private.config.json` 只保存本地开发者工具偏好和预览场景，已从上传包中排除。

## 当前能力

- 首页展示植物健康概览、今日待查、重点观察和平均健康分
- 支持植物详情页，展示封面、评分、养护特征、光照、浇水、关注点和备注
- 详情页支持更换封面，并可回到首页自动打开对应植物的编辑弹窗
- 支持按植物家族筛选、按评分排序
- 支持编辑评分、阶段、关注点和备注
- 支持给每盆植物上传封面图
- 支持我的植物档案：拍照后先展示识别或诊断结果，用户确认后保存到植物档案和生命周期记录
- 支持拍照识别、健康诊断和 AI 养护策略占位，后续可基于每次打卡照片接入云函数
- 支持隐私与数据说明页，解释相机、相册、本地缓存和剪贴板用途
- 支持复制本地备份和从剪贴板恢复本地档案
- 编辑内容保存在微信小程序本地缓存里
- 支持小程序分享卡片，分享图使用本地头图资源
- 已在 `app.json` 配置相机权限说明，并在上传封面前兼容触发微信隐私授权

## 本地预览

1. 打开微信开发者工具
2. 选择「导入项目」
3. 项目目录选择当前文件夹
4. AppID 先使用测试号，或把 `project.config.json` 里的 `appid` 改成你自己的正式小程序 AppID
5. 编译后即可预览

导入开发者工具前，也可以在当前目录运行：

```bash
npm run check
```

这会检查小程序配置、关键文件、植物数据、核心筛选统计、本地备份逻辑、隐私授权分支、样式兼容风险和估算上传包体积。

如需查看预计会进入上传包的文件列表，可以运行：

```bash
npm run report:package
```

替换正式 AppID 后，上传代码前运行：

```bash
npm run set:appid -- wx1234567890abcdef
```

然后运行：

```bash
npm run check:release
```

这会额外检查 `project.config.json` 里的 AppID 不再是测试值。

## 上线前必须处理

1. 到微信公众平台注册小程序，拿到正式 AppID
2. 执行 `npm run set:appid -- wx1234567890abcdef`，把 `project.config.json` 里的 `appid` 从 `touristappid` 改为正式 AppID
3. 在微信开发者工具里完成真机预览
4. 如果只做本地档案，不需要配置服务器域名
5. 如果要多设备同步，需要接入云开发数据库或后端接口，再在小程序后台配置合法域名
6. 上传代码并在微信公众平台提交审核

提交前可以对照 `MINIPROGRAM-SUBMISSION-CHECKLIST.md`，并从 `WECHAT-SUBMISSION-COPY.md` 复制提审文案。小程序头像和介绍封面素材放在 `miniprogram-assets/`，该目录已从代码上传包中排除。

## 审核前自查

- 首页首屏能正常显示头图、健康分和植物卡片
- 当前目录执行 `npm run check` 通过
- 替换正式 AppID 后执行 `npm run check:release` 通过
- 从首页点击植物“详情”能进入详情页，并能分享该植物档案
- 详情页更换封面后，返回首页能看到新封面
- 详情页点击“回首页编辑”后，首页能自动打开对应植物的编辑弹窗
- 筛选、排序、编辑保存都能在真机预览中正常使用
- 上传封面时，相册选择和相机拍摄至少各测试一次
- 首页底部“隐私说明”能打开隐私与数据说明页
- 隐私页“复制本地备份”和“从剪贴板恢复”可用
- 关闭并重新打开小程序后，编辑内容和封面仍能保留
- 从右上角菜单分享给自己或测试微信，分享卡片能打开首页
- 小程序名称、头像、服务类目、隐私保护指引在微信公众平台后台填写完整
- 如果后续接云开发或后端接口，必须先配置合法域名，再提交审核

## 当前边界

当前版本是本地档案版，数据只保存在当前微信客户端里。拍照会先生成识别或诊断结果，用户确认保存后，照片才会记录到该植物的生命周期时间线。拍照识别、健康诊断和 AI 养护策略当前为体验占位，正式接入云函数或后端前不会把照片上传到服务器。卸载小程序、清理缓存或换手机后，本地修改可能丢失。可以在隐私与数据说明页复制本地备份文本，并在需要时从剪贴板恢复。首页底部的“清空本地修改”会清理本地档案和相关临时状态。正式给多人或多设备使用前，建议升级到云开发数据库。

## 后续建议

- 把本地缓存升级为云开发数据库，实现换手机不丢数据
- 增加浇水记录、换盆记录、照片时间线
- 在详情页增加照片时间线和养护记录
- 增加封面重新选择提醒，帮助用户在换设备后修复本地封面路径
